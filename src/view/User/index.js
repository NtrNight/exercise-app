import React, { Component } from 'react';
import { AppBar, Main } from '../../components/CreateUser';

export default class CreateUser extends Component {
  render() {
    return(
        <div>
            <AppBar />
            <Main />
        </div>
    );
  }
}
