import React, { Component } from 'react';
import { AppBar, Main } from '../../components/CreateExercise';

export default class CreateExercise extends Component {
  render() {
    return(
        <div>
            <AppBar />
            <Main />
        </div>
    );
  }
}
